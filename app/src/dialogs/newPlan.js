import React from 'react';

import DialogWrapper from './dialogWrapper';

const NewPlan = (props) => {
  return (
    <DialogWrapper
      {...props}
    >
      <p>hello</p>
    </DialogWrapper>
  );
};

export default NewPlan;
