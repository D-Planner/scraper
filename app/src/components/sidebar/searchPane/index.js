import React from 'react';

import './searchPane.scss';

const SearchPane = (props) => {
  return (
    <div className="search pane active">
      <h1 className="pane-header">Search</h1>
    </div>
  );
};

export default SearchPane;
